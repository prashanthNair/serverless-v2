export enum ResponseMessage {
  ERROR = "Unknown error.",
  INVALID_REQUEST = "Invalid Request!",
  GET_ITEM_ERROR = "Item does not exist",
  CREATE_USER_FAILED = "Issue while creating user",
  SUCCESS = "Success",
}
